server:
  port: ${PORT:8015}

pas:
  service:
    url: ${PAS_SERVICE_URL:https://oipaprod.britam.com:5443}

auth:
  service:
    url: ${AUTH_SERVICE_URL:http://10.20.9.21:9097}

spring:
  datasource:
    secondary-dev:
      jdbc-url: ${JDBC_URL:jdbc:oracle:thin:@//10.20.9.21:1521/OIPAPROD}
      username: ${DB_USER:OIPA_PAS}
      password: ${DB_PASSWORD:OIPA_PAS}
      driver-class-name: oracle.jdbc.OracleDriver
      maximum-pool-size: 30
